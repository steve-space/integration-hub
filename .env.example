# URLs of the two backend systems this integration layer connects to.
# In production these would point at real internal services; for local
# development they point at the mock systems included in this repo.
WAREHOUSE_SYSTEM_URL=http://localhost:9001
PRICING_SYSTEM_URL=http://localhost:9002
BACKEND_TIMEOUT_SECONDS=5.0
